// Package version stores contant values which would be replaced during compile,keep it as-is when developing is good
package version

// Version version
const Version = "DEV"

// ReleaseTag git branch(tag is better)
const ReleaseTag = "DEV"

// CommitID git commit id(full version)
const CommitID = "DEV"

// ShortCommitID git commit id(short version)
const ShortCommitID = "DEV"
